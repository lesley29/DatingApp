import { ImageWithFallbackDirective } from './image-fallback.directive';

describe('ImageFallbackDirective', () => {
    it('should create an instance', () => {
        const directive = new ImageWithFallbackDirective();
        expect(directive).toBeTruthy();
    });
});
